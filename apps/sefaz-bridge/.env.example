# Porta local (default 8788)
PORT=8788

# Obrigatório em produção — mesmo valor no campo "Token" da conexão sefaz-dfe (Bearer)
BRIDGE_TOKEN=troque-por-um-segredo-forte

# Timeout da chamada à SEFAZ (ms)
REQUEST_TIMEOUT_MS=60000
